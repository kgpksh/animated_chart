const polarAnimations = {
    default : (duration, type, easing, indexAxis = 'y') => {
        return {
          duration: duration,
          type : type,
          easing : easing,
        }
    },

}

export default polarAnimations